name: Hostvault panel contracts

on:
  push:
    branches:
      - 'chore/panel-upstream-*'
  pull_request:
    branches:
      - 'hostvault/**'

permissions:
  contents: read

concurrency:
  group: hostvault-panel-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  contracts:
    name: PHP ${{ matrix.php }} / MySQL 8.4.9
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        php: ['8.2', '8.3']
    services:
      database:
        image: mysql:8.4.9
        env:
          MYSQL_ROOT_PASSWORD: panel-ci-only
          MYSQL_DATABASE: testing
        ports:
          - 3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: bcmath, curl, gd, mbstring, pdo_mysql, pcntl, posix, xml, zip
          tools: composer:v2
          coverage: none
      - name: Configure isolated test environment
        run: cp .env.ci .env
      - name: Install locked dependencies
        run: composer install --no-interaction --no-progress --prefer-dist
      - name: PHP style
        run: vendor/bin/php-cs-fixer fix --dry-run --diff
      - name: Unit tests
        run: vendor/bin/phpunit --fail-on-risky --bootstrap vendor/autoload.php tests/Unit
        env:
          DB_HOST: UNIT_NO_DB
      - name: Integration and fork contract tests
        run: vendor/bin/phpunit --fail-on-risky tests/Integration
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: ${{ job.services.database.ports[3306] }}
          DB_DATABASE: testing
          DB_USERNAME: root
          DB_PASSWORD: panel-ci-only
